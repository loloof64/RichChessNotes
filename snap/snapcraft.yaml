name: rich_chess_notes
version: 0.1.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-rich_chess_notes: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.rich_chess_notes # adjust accordingly to your app name

apps:
  rich_chess_notes:
    command: rich_chess_notes
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-rich_chess_notes
parts:
  rich_chess_notes:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
